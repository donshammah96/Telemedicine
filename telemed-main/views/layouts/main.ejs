<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="Content-Security-Policy" 
      content="default-src 'self'; script-src 'self' https://code.jquery.com https://cdnjs.cloudflare.com https://maxcdn.bootstrapcdn.com; style-src 'self' https://fonts.googleapis.com https://maxcdn.bootstrapcdn.com 'unsafe-inline'; font-src 'self' https://fonts.gstatic.com;">

  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <title><%= typeof title !== 'undefined' ? title : "TeleMed - Your Healthcare Anywhere" %></title>
  <% if (typeof doctor !== 'undefined') { %>
        <meta name="doctor-id" content="<%= doctor.id %>">
    <% } %>
  <% if (typeof patient !== 'undefined') { %>
        <meta name="patient-id" content="<%= patient.id %>">
    <% } %>
  <% if (typeof admin !== 'undefined') { %>
        <meta name="admin-id" content="<%= admin.id %>">
    <% } %>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/dashboard.css">
  <script src="/js/register.js"></script>
  <% if (typeof styles !== 'undefined') { %>
      <%- styles %>
  <% } %>
</head>
<body>
  <%- include('../partials/header.ejs') %>

  <main>
    <%- body %>
  </main>

  <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
  <script src="/js/register.js"></script>
  <% if (typeof scripts !== 'undefined') { %>
      <%- scripts %>
  <% } %>

  <%- include('../partials/footer.ejs') %>
</body>
</html>
